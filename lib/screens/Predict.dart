import 'package:flutter/material.dart';

class PredictPage extends StatelessWidget {
  const PredictPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text('Predict',style: TextStyle(fontSize: 60),),
        ),
    );
  }
}
