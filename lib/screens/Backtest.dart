import 'package:flutter/material.dart';

class BacktestPage extends StatelessWidget {
  const BacktestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text('BackTest',style: TextStyle(fontSize: 60),),
        ),
    );
  }
}
